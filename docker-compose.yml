services:
  klgpff-bot:
    build: .
    restart: unless-stopped
    env_file: .env
    depends_on:
      - qdrant
    volumes:
      - ./facts:/app/facts
      - ./prompts:/app/prompts
      - ./models:/app/models
      - ./outputs:/app/outputs
      - ./backups:/app/backups

  qdrant:
    image: qdrant/qdrant:latest
    restart: unless-stopped
    ports:
      - "127.0.0.1:6333:6333"
    volumes:
      - qdrant_storage:/qdrant/storage

volumes:
  qdrant_storage:
